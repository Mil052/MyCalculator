#Description
Hi everyone,
Thats my first app buid in React. Main class component is src/pages/calculator.js (this component contains
all of the logic). I also make some effort to style the calculator buttons and “display” with smoothly rounded corners (similar to squircle corners) (src/pages/calculator.scss)

## How to run app
1.Instal dependencies:
    $ npm install
2. Run project with:
    $ npm start

